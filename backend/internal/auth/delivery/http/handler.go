// Package authhttp is intentionally empty.
// Authentication is handled by Supabase Auth — the mobile app signs in
// directly via the Supabase SDK. The Go API only validates the resulting
// Supabase JWT via the JWTAuth middleware.
package authhttp
